<UserControl x:Class="JobAlertManagerGUI.View.EMailMetaDetails"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="300" d:DesignWidth="300" Loaded="OnLoaded" DataContextChanged="OnDataContextChanged" MinHeight="300">
    <UserControl.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <ResourceDictionary Source="Resources/ControlStyles.xaml" />
            </ResourceDictionary.MergedDictionaries>
        </ResourceDictionary>
    </UserControl.Resources>
    <StackPanel>
        <GroupBox x:Name="GBA" Margin="0,1" >
            <TextBlock x:Name="TxtReplyTo" TextWrapping="WrapWithOverflow" />
        </GroupBox>
        <GroupBox x:Name="GBC" Margin="0,1" >
            <TextBlock x:Name="TxtRouting" TextWrapping="WrapWithOverflow" />
        </GroupBox>
        <GroupBox x:Name="GBD" Margin="0,1" >
            <TextBlock x:Name="TxtMDN" TextWrapping="WrapWithOverflow" />
        </GroupBox>
        <GroupBox x:Name="GBE" Margin="0,1" >
        </GroupBox>
    </StackPanel>
</UserControl>
